---
import BaseLayout from "~/layouts/BaseLayout.astro";
import UnauthorizedMessage from "@/components/composed/UnauthorizedMessage/UnauthorizedMessage.astro";

const reason = Astro.url.searchParams.get("reason") ?? undefined;
---

<BaseLayout meta={{ title: "Unauthorized" }}>
  <UnauthorizedMessage reason={reason} />
</BaseLayout>
